# spl-token-examples
Minimal Rust-based CLI examples to mint and transfer SPL tokens (conceptual).
These are small Rust programs that use the Solana SDK; for production use, prefer using Anchor or JS SDK.
